# user-data-transformer
## For running tests:
### - Install npm
### - Install jest:
### - npm init -y
### - npm install --save-dev jest
## if npm version is lower than 9 you should run:
###  - npm set-script test "jest"
## if npm version is greater you should run:
###  - npm pkg set scripts.test="jest"
